# Test ROMs (dev only)

`nestest.nes` — CPU/PPU test ROM by kevtris, public-domain, ubiquitous in NES emulator development/testing (sourced from [christopherpow/nes-test-roms](https://github.com/christopherpow/nes-test-roms)). Used only for the F1.13 end-to-end pipeline check (see the "Dev: correr ROM de prueba" button in Ajustes, `__DEV__`-gated). Not part of the homebrew catalog, not shipped/advertised to end users.
